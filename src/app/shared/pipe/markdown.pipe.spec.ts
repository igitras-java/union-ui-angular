/* tslint:disable:no-unused-variable */

import { MarkdownPipe } from './markdown.pipe';

describe('Pipe: Markdowne', () => {
    it('create an instance', () => {
        let pipe = new MarkdownPipe();
        expect(pipe).toBeTruthy();
    });
});
